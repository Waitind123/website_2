DATABASE_URL="file:./dev.db"
SESSION_SECRET="replace-with-long-random-string-at-least-32-chars"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
STRIPE_PRICE_PRO=""
RESEND_API_KEY=""
RESEND_FROM_EMAIL="Lumina <no-reply@yourdomain.com>"

# 生产部署（Vercel / 公网）建议使用 PostgreSQL，例如 Neon：
# DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
# 然后把 prisma/schema.prisma 里 datasource db 的 provider 改成 postgresql 并执行 db push / migrate
